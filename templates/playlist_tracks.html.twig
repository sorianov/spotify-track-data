{# {{ dump(params) }} #}
{# {{ dump(playlistTracks) }} #}
{# {{ dump(tracks) }} #}

{% for track in tracks %}
    <div>
        <header>{{ track.track.name }}
        </header>
        <img src="{{ track.track.album.images.2.url }}" />
    </div>
    {# <div>{{ track.track }}</div> #}
{% endfor %}